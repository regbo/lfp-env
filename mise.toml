[env]
PROJECT_NAME = "{{ config_root | basename }}"

[tasks.commit]
run = "uv run --with-requirements requirements.txt -- workflow/deploy.py commit"

[tasks.tag]
run = "uv run --with-requirements requirements.txt -- python workflow/deploy.py tag"
